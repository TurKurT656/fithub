import 'package:flutter/material.dart';

import '../../../library/uicore/uicore.dart';

class SplashScreen extends BasePage {
  static const url = '/splash';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(),
    );
  }
}
